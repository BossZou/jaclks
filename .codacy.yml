version: "2.0.0"  # Version

exclude_paths:
  - "**/thirdparty/**"  # exclude third-party libs

engines:
  cppcheck:
    enabled: true
    exclude:
      - "missingInclude"    # 忽略头文件未找到错误
      - "missingIncludeSystem"

#    config:
      # 指定 C++ 标准
#      std: c++17
#      # 使用抑制文件
#      suppressions: "cppcheck-suppressions.txt"
